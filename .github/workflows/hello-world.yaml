name: Node App Git Actions
on:
  push:
    branches:
      - develop
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World Action
        run: echo "Hello World from GitHub Actions!"